class Fragrance:
	def __init__(self, url: str, name: str, brand: str, gender: str, rating: float, decade: str, description: str):
		self.url = url
		self.name = name
		self.brand = brand
		self.gender = gender
		self.rating = rating
		self.decade = decade
		self.description = description